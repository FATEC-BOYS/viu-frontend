/*
 * A home da área de administração.
 *
 * O conteúdo mora em `components/dashboard/PainelDoAdmin` porque é o mesmo
 * painel que o `/dashboard` de um ADMIN mostra — e um painel servido de dois
 * lugares não pode ser uma página.
 */
export { default } from '@/components/dashboard/PainelDoAdmin'
